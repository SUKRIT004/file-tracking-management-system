# Frontend

The user interface is built with React, Vite, Tailwind CSS, and reusable UI components.

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run build
```

Run these commands from this directory. The production build is written to `dist/`, which is intentionally ignored by Git.

For full project setup, see the repository root README.
